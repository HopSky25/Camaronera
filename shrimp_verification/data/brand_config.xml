<odoo>
  <!-- Instala el logo y el favicon de Trazul en cada install/update.
       Es idempotente y no destructivo: solo reemplaza la imagen por defecto
       de Odoo, nunca una que el cliente haya subido. -->
  <function model="website" name="_shrimp_ensure_brand"/>
</odoo>
